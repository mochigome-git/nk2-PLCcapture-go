version: "3.8"

services:
  nk2-msp:
    container_name: nk2-msp
    image: nk2-msp:1.6v
    restart: always
    stop_grace_period: 30s
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "10"
